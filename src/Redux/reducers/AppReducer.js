//appReducer
const initialState = {
    // theme: 'blue'
    notificationMessage:'',
    showNotification:true
}

export const AppReducer = (state = initialState , action) => {
   
    switch(action.type){
            
        default : 
            return state
    }

}

export default AppReducer;