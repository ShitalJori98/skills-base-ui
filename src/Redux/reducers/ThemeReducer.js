
const initialState = {
    theme: 'blue'
    
}

export const ThemeReducer = (state = initialState , action) => {
   
    switch(action.type){
            
        default : 
            return state
    }

}

export default ThemeReducer;